# Khi chạy dev local (npm run dev): để trống, Vite proxy sẽ trỏ về 127.0.0.1:9100
# Khi deploy Vercel: đặt bằng URL HTTPS công khai của backend (Cloudflare Tunnel)
VITE_API_BASE_URL=
VITE_SOCKET_URL=
